apiVersion: networking.istio.io/v1alpha3
kind: ServiceEntry
metadata:
  name: weatherapi
spec:
  hosts:
    - api.weatherapi.com
  ports:
    - number: 80
      name: http-port
      protocol: HTTP
    - number: 443
      name: https
      protocol: HTTPS
  resolution: DNS
---
apiVersion: networking.istio.io/v1alpha3
kind: Gateway
metadata:
  name: weather-egressgw
spec:
  selector:
    istio: egressgateway
  servers:
    - port:
        number: 80
        name: http
        protocol: HTTP
      hosts:
        - weatherapi.local
---
apiVersion: networking.istio.io/v1alpha3
kind: DestinationRule
metadata:
  name: weather-egressgw
spec:
  host: weather-egressgw.istio-system.svc.cluster.local
  subsets:
    - name: weatherapi
---
apiVersion: networking.istio.io/v1alpha3
kind: VirtualService
metadata:
  name: weather-vservice
spec:
  hosts:
    - weatherapi.local
  gateways:
    - weather-egressgw
    - mesh
  http:
    - match:
        - gateways:
            - mesh
          port: 80
      route:
        - destination:
            host: weather-egressgw.istio-system.svc.cluster.local
            subset: weatherapi
            port:
              number: 80
          weight: 100
    - match:
        - gateways:
            - weather-egressgw
          port: 80
      route:
        - destination:
            host: weatherapi.local
            port:
              number: 80
          weight: 100